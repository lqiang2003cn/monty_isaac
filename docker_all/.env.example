# Copy to .env and fill in. .env is gitignored; do not commit passwords.
# Used when running: docker compose --profile real up (from docker_all/)

# Robot ZMQ service
ZMQ_HOST=192.168.31.142
ZMQ_PORT=5555

# Optional: auto-start ZMQ service on robot via SSH when probe fails (runs in conda env gRPC by default)
# ZMQ_REMOTE_SSH=wheeltec@192.168.31.142
# ZMQ_REMOTE_SSH_PASSWORD=
# ZMQ_REMOTE_ENV=gRPC
