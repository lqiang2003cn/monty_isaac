"""monty_ext — LQTech extensions for tbp.monty.

This package extends Monty with custom sensor modules, environment interfaces,
and experiment configs for the turntable scanning + real-world recognition
pipeline.  It imports from tbp.monty but never modifies it, so upstream
updates can be pulled without conflicts.
"""
