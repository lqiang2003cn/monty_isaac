# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
*.egg

# Virtual envs (use a path if your venv lives inside the repo)
*venv/
.venv/
env/

# IDE / editor
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Isaac Sim / Omniverse (cache, logs)
*.log
.cache/

# ROS2 / colcon
build/
install/
log/

# Generated URDF (regenerate via scripts/generate_isaac_urdf.sh)
docker_all/shared/x3plus_isaac/urdf/x3plus_isaac.urdf

# Debug logs (bind-mounted from container /var/log/monty/)
docker_all/logs/

# Local env (passwords, host overrides) — do not commit
.env
docker_all/.env

# Monty data and results (large, downloaded at runtime)
docker_all/data/
